# Usage
```bash
git clone https://github.com/project-openwrt/squashfs-tool && cd squashfs-tool
./modify-firmware.sh extract you_firmware_name.bin # Extract a firmware
./modify-firmware.sh create new_firmware_name.bin # Create a firmware
```
